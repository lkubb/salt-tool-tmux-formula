# vim: ft=yaml
#
# Set default values.
---
values:
  lookup:
    pkg:
      name: tmux
    paths:
      confdir: ''
      conffile: '.tmux.conf'
      xdg_dirname: 'tmux'
      xdg_conffile: 'tmux.conf'
    rootgroup: root
  tofs:
    files_switch:
      - G@id|U@users
      - id
      - C@roles|U@users
      - roles
      - G@os_family|U@users
      - os_family
      - U@users
...
